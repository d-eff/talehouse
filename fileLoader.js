const barkeep = require('./data/barkeep.json'),
      story = require('./data/story.json'),
      tellMe = require('./data/tellMe.json'),
      truth = require('./data/truth.json')

module.exports = {
  barkeep: barkeep.data,
  story: story.data,
  tellMe: tellMe.data,
  truth: truth.data
};